$(document).ready(function () {
    $(".zoom").imagezoomsl({
        zoomrange: [2, 8],
        zoomstart: 0,
        innerzoommagnifier: true,
        classmagnifier: 'glass',
        magnifiersize: [150, 150]
    });
});